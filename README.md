# CLPsych.org Website

Build the site using `make`. You may need to first install dependencies with `pip install -r requirements.txt`.

TODO:

 - [ ] Missing old shared task pages
 - [ ] Any other old pages?
 - [ ] Github Action -> Github Site
 - [ ] Update styles
 - [ ] Clean up static content -- what's actually needed?
 - [ ] Social media feed
 - [ ] Domain Name
